---
description: openTrattOS project dispatcher — always read AGENTS.md first.
alwaysApply: true
---

# openTrattOS — Cursor dispatcher

Cursor: read [AGENTS.md](../../AGENTS.md) first. That is the project dispatcher.

Follow its **bootstrap directive** (section 0) before any task. For universal norms, `AGENTS.md` inherits from `.ai-playbook/specs/*` (git submodule).

This rule file is a thin router so Cursor matches the behavior of Claude Code and Gemini CLI. All logic lives in `AGENTS.md`.
